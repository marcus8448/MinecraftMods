{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "entrypoints": {
    "main": [
      "io.github.marcus8448.template.TemplateFabric"
    ]
  },
  "depends": {
    "fabricloader": ">=0.14",
    "minecraft": ">=${mc_major_minor}",
    "java": ">=${java_version}"
  }
}
